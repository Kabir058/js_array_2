 // //task1
// let clr =[];
let colors=['red','blue','green','yellow']
for(let i= colors.length-1; i>=0; i--)
{
    let clr = colors[i]
    console.log(clr)
}

// let clr = []; // Empty array to store reversed colors
// let colors = ['red', 'blue', 'green', 'yellow'];

// for (let i = colors.length - 1; i >= 0; i--) {
//     clr.push(colors[i]); // Push each item into 'clr'
// }

// console.log(clr); // ['yellow', 'green', 'blue', 'red']


// let colors = ['red', 'blue', 'green', 'yellow'];

// for (let i = colors.length - 1; i >= 0; i--) {
//     console.log(colors[i]);
// }
